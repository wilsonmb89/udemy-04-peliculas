export enum Idioma {
  español = 'es',
  ingles = 'en-US'
}

export enum Region {
  colombia = 'CO',
  eeuu = 'US'
}

export enum TipoOrdenamiento {
  popularidadDesc = 'popularity.desc',
  popularidadAsc = 'popularity.asc',
  voteAverageDesc = 'vote_average.desc',
  voteAverageAsc = 'vote_average.asc'
}

export enum ParametrosUtils {
  movie = 'movie',
  credits = 'credits',
  search = 'search',
  genre = 'genre',
  list = 'list'
}
